# Build artifacts and local state — never needed in the image build context.
node_modules
dist
target
.git

# Local dev stack + volumes
dev/garage_meta
dev/garage_data

# Editor / OS cruft
.DS_Store
*.log
