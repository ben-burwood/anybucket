{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Read-only S3 explorer: window chrome, save-file dialog, clipboard copy, open URLs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-save",
    "clipboard-manager:allow-write-text",
    "opener:allow-open-url"
  ]
}
