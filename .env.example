# REQUIRED. Passphrase used to encrypt S3 secret access keys at rest
# (argon2id key derivation + AES-256-GCM).
# Use a long, random value.
# # WARNING: losing this makes every stored secret unrecoverable — back it up somewhere safe, separate from the /config volume.
ANYBUCKET_MASTER_KEY=
